name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v0.1.0)'
        required: true
        default: 'v0.1.0'

permissions: read-all

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  release:
    name: Verify and Publish Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Set up Go
        uses: actions/setup-go@f111f3307d8850f501ac008e886eec1fd1932a34 # v5.3.0
        with:
          go-version: '1.24.x'
          cache: false

      - name: Determine Release Tag
        id: tag_info
        run: |
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "Release target tag: $TAG"

      - name: Verify Immutable Release Artifact
        id: verify_release
        env:
          GIT_TAG: ${{ steps.tag_info.outputs.tag }}
        run: ./scripts/verify_release.sh "$GIT_TAG"

      - name: Upload release verification manifest
        if: always()
        uses: actions/upload-artifact@4cec3d8aa04e39d1a68397de0c4cd6fb9dce8ec1 # v4.6.1
        with:
          name: release-manifest-${{ steps.tag_info.outputs.tag || github.run_id }}
          path: release-manifest.json
          if-no-files-found: warn
          retention-days: 90

      - name: Run Quality Gates
        id: quality_gates
        run: ./scripts/check_coverage.sh

      - name: Verify Published Module from an Isolated Consumer
        id: isolated_consumer
        env:
          TAG: ${{ steps.tag_info.outputs.tag }}
        run: |
          SCRATCH_DIR=$(mktemp -d)
          trap 'rm -rf "$SCRATCH_DIR"' EXIT
          echo "Testing consumer build in isolated directory: $SCRATCH_DIR with tag ${TAG}"
          cd "$SCRATCH_DIR"
          GOWORK=off go mod init scratchconsumer
          GOWORK=off go get "github.com/umesh0492/go-fintech-india@${TAG}"
          cat << 'EOF' > main.go
          package main

          import (
          	"fmt"
          	fintechin "github.com/umesh0492/go-fintech-india"
          )

          func main() {
          	m := fintechin.NewMoney(10000)
          	fmt.Println("Money:", m.String())
          }
          EOF
          GOWORK=off go build -v .
          ./scratchconsumer
          echo "✅ Published isolated scratch consumer verification passed."

      - name: Create release or replace its verification manifest
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.tag_info.outputs.tag }}
        run: |
          test -s release-manifest.json
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "GitHub release $TAG already exists; replacing only its verification manifest asset."
            gh release upload "$TAG" release-manifest.json --clobber
            exit 0
          fi
          echo "Creating release $TAG with its verification manifest..."
          gh release create "$TAG" release-manifest.json \
            --title "go-fintech-india $TAG" \
            --notes-file <(awk '/^## \[/{n++} n==1' CHANGELOG.md)
